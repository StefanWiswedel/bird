# Referenced by build.gradle.kts's release buildType. Empty: isMinifyEnabled is false,
# so no rules are needed yet; kept so the proguardFiles() reference resolves to a real
# file if minification is ever turned on.
